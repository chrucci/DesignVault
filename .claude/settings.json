{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "git checkout main && git pull origin main",
            "statusMessage": "Pulling latest changes from main..."
          }
        ]
      }
    ]
  }
}
